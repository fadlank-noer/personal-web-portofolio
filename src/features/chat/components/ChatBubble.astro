---
/** Canonical mini moved to src/components/ui/chat/ChatBubble.astro — this is shim for feature compat */
import Canonical from '../../../components/ui/chat/ChatBubble.astro';
import type { MediaItem } from '../../../lib/types/chat';
interface Props { role: 'user' | 'assistant'; content: string; images?: MediaItem[]; timestamp?: number; isStreaming?: boolean; avatarLabel?: string; class?: string; }
const props = Astro.props as Props;
---
<Canonical {...props} />