---
/** Canonical mini moved to src/components/ui/chat/ChatResponse.astro — shim */
import Canonical from '../../../components/ui/chat/ChatResponse.astro';
import type { MediaItem } from '../../../lib/types/chat';
interface Props { answer: string; images?: MediaItem[]; answer_type?: string; isStreaming?: boolean; messageId?: string; class?: string; }
const props = Astro.props as Props;
---
<Canonical {...props} />
